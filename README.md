## Environment setup
If webpack is not installed

```sh
  $ npm webpack -g
```
then:

```sh
  $ cd code-test
```

```sh
  $ npm install
```

## Development

Start webpack server to view:

```sh
  $ npm start
```
t

Open [http://localhost:3001](http://localhost:3001)
